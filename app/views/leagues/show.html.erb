<div class="row">
  <%= render 'sidebar' %>
  <div class="span6">
    <p id="notice"><%= notice %></p>
    <h1>
      <%= @league.name %>
    </h1>

    <p>
      <%= @league.description %>
    </p>

    <p>
      <b>Clubs</b> 
      <span class="badge badge-info"><%= @league.clubs.count %></span>
      <br/>
      <b>Divisions</b>
      <span class="badge badge-info"><%= @league.divisions.count %></span>
    </p>
    <% if current_user == nil %>
      <!-- Viewing as anonymous -->
    <% else %>
    <% if current_user.admin? %>
      <%= link_to 'Edit League', edit_league_path(@league) %>
    
      <%= form_for [@league, Club.new] do |f| %>
        <%= f.label :name %>
        <%= f.text_field :name %><br/>
        <%= f.label :description %>
        <%= f.text_field :description %><br/>
        <%= f.submit %>
      <% end %>

      <%= form_for [@league, Division.new] do |d| %>
        <%= d.label :name %>
        <%= d.text_field :name %><br/>
        <%= d.submit %>
      <% end %>
    <% end %>
    <% end %>

    <ul>
      <%= render @posts %>
    </ul>

  </div> <!-- span -->
</div> <!-- row -->
